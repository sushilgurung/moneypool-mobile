import { View, Text } from 'react-native';

/**
 * Contact Screen where user can add their contacts
 * @returns {JSX.Element} Contact Screen
 */

export default function Contact() {
  return (
    <View className="flex-1 bg-blue-200">
      <Text>Contact</Text>
    </View>
  );
}
